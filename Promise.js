// Understanding about promises

// -It is an object
// - It is the solution to callback hell

// PROMISE
// -pending
// -fulfilles (Resolved)
// -reject

// example:

let promise = new promise((resolve, reject) =>{
    console.log("This is a promise.")
    // resolve("Sucess");
    // reject("Error Occured")
});
