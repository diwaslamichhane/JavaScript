// Understanding forEach Loops

/*

Suppose, let a = [1, 2, 3, 4, 5, 6];
If you want to modify each elements the forEach loop is used.

Example:

*/

let a = [1, 2, 3, 4, 5];
a.forEach(function(val){
    console.log(val+2); //2 will be added in every elements of the array
});
